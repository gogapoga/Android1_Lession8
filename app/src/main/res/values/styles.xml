<resources>
    <!-- Base application theme. -->
    <style name="DayTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDayPrimary</item>
        <item name="colorPrimaryDark">@color/colorDayPrimaryDark</item>
        <item name="colorAccent">@color/colorDayAccent</item>
    </style>

    <style name="DayTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DayTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="DayTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    !-- Base application theme. -->
    <style name="NightTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNightPrimary</item>
        <item name="colorPrimaryDark">@color/colorNightPrimaryDark</item>
        <item name="colorAccent">@color/colorNightAccent</item>
    </style>

    <style name="NightTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NightTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="NightTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
